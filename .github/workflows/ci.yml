name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Run tests
        run: PYTHONPATH=src python -m unittest discover -s tests
      - name: Build wheel
        run: python -m pip wheel . --no-deps --no-build-isolation -w dist
      - name: Verify wheel
        run: python scripts/verify_wheel.py dist/unit27_proof_ledger-0.1.0-py3-none-any.whl
